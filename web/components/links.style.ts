import styled from 'styled-components';
export const LinkToPages = styled.div`
  color: var(--black);
  font-size: 1.5rem;
  font-family: inherit;
  margin-top: 3rem;
  text-decoration: underline;
  cursor: pointer;
`;
